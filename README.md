
# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     es la base por la cual se va a multiplicar [número] [requerido]
  -h, --hasta    rango de numeros para multiplicar        [número] [defecto: 10]
  -l, --listar   Muestra la tabla en consola         [booleano] [defecto: false]

```